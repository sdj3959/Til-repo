## 1102 Java 고급: 디자인 패턴과 SOLID 원칙

---

### ✅ 1. 디자인 패턴 (Design Patterns)

*   **디자인 패턴**이란 소프트웨어 설계에서 자주 발생하는 문제들에 대한 **검증된, 재사용 가능한 해결책**입니다. 코드를 어떻게 구조화해야 더 유연하고, 확장성 있으며, 유지보수하기 좋을지에 대한 **모범 사례(Best Practice)이자 설계의 청사진**입니다.

#### ➕ 대표적인 디자인 패턴 예시

1.  **싱글턴 패턴 (Singleton Pattern)**:
    *   **개념**: 클래스의 인스턴스가 **오직 하나만** 생성되도록 보장하는 패턴입니다.
    *   **구현**: 생성자를 `private`으로 막고, `static` 메서드(`getInstance()`)를 통해서만 유일한 인스턴스에 접근할 수 있도록 합니다.
    *   **Spring과의 관계**: Spring 컨테이너가 관리하는 Bean들은 기본적으로 **싱글턴 스코프(Singleton Scope)**로 동작합니다. Spring이 이 패턴을 관리해주므로, 개발자가 직접 싱글턴 코드를 작성할 일은 거의 없습니다.

2.  **팩토리 패턴 (Factory Pattern)**:
    *   **개념**: 객체를 생성하는 로직을 별도의 **"공장(Factory)"** 클래스로 캡슐화하여, 클라이언트 코드가 구체적인 클래스에 직접 의존하지 않도록 만드는 패턴입니다.
    *   **장점**: 새로운 종류의 객체가 추가되더라도, 클라이언트 코드의 변경 없이 팩토리 클래스만 수정하면 되므로 유연성과 확장성이 높아집니다.

3.  **스트래티지 패턴 (Strategy Pattern)**:
    *   **개념**: 다양한 **"전략(알고리즘)"**들을 각각의 클래스로 캡슐화하고, 런타임에 필요에 따라 전략을 동적으로 교체할 수 있도록 하는 패턴입니다.
    *   **예시**: 결제 시스템에서 '신용카드 결제 전략', '카카오페이 결제 전략' 등을 각각의 클래스로 만들고, 사용자의 선택에 따라 결제 전략 객체를 바꿔 끼우는 방식입니다. 다형성의 대표적인 활용 사례입니다.

---

### ✅ 2. SOLID 원칙: 좋은 객체 지향 설계의 5가지 원칙

*   **SOLID**는 객체 지향 프로그래밍에서 **유지보수하기 좋고, 유연하며, 이해하기 쉬운** 소프트웨어를 만들기 위해 지켜야 할 5가지 설계 원칙의 앞 글자를 딴 것입니다. Spring과 같은 현대적인 프레임워크는 이 원칙들을 깊이 반영하고 있습니다.

| 원칙 | 이름 | 설명 |
| :--- | :--- | :--- |
| **S** | **단일 책임 원칙**<br>(Single Responsibility Principle) | 클래스는 **단 하나의 책임**만 가져야 한다. (클래스를 변경해야 할 이유는 단 하나여야 한다.) |
| **O** | **개방-폐쇄 원칙**<br>(Open-Closed Principle) | 소프트웨어 요소(클래스, 모듈 등)는 **확장에는 열려** 있어야 하고, **수정에는 닫혀** 있어야 한다. |
| **L** | **리스코프 치환 원칙**<br>(Liskov Substitution Principle) | 자식 클래스는 언제나 부모 클래스의 역할을 대체할 수 있어야 한다. (상속은 신중하게) |
| **I** | **인터페이스 분리 원칙**<br>(Interface Segregation Principle) | 클라이언트는 자신이 사용하지 않는 메서드에 의존해서는 안 된다. (인터페이스는 역할에 맞게 잘게 쪼개라) |
| **D** | **의존관계 역전 원칙**<br>(Dependency Inversion Principle) | **"구체적인 것(구현 클래스)에 의존하지 말고, 추상적인 것(인터페이스)에 의존하라."** |

---

### ✅ 3. SOLID와 스프링의 핵심: 의존관계 역전 원칙(DIP)과 의존성 주입(DI)

*   SOLID 원칙 중 **의존관계 역전 원칙(DIP)**은 Spring 프레임워크의 가장 핵심적인 사상인 **의존성 주입(DI)**과 **제어의 역전(IoC)**의 이론적 기반이 됩니다.

#### ➕ 의존관계 역전 원칙 (DIP)

*   **나쁜 설계**: `OrderService`가 `KakaoPay`라는 **구체적인 구현 클래스**에 직접 의존하는 것.
    ```java
    public class OrderService {
        private KakaoPay kakaoPay = new KakaoPay(); // 구체 클래스에 직접 의존 (DIP 위반)
        
        public void processPayment() {
            kakaoPay.pay();
        }
    }
    ```
    *   **문제점**: 결제 방식을 `NaverPay`로 바꾸려면 `OrderService` 코드를 직접 수정해야 합니다. (OCP 위반)

*   **좋은 설계 (DIP 적용)**: `OrderService`가 `PaymentGateway`라는 **추상적인 인터페이스**에 의존하고, 실제 구현체는 외부에서 주입받는 것.
    ```java
    public class OrderService {
        private final PaymentGateway paymentGateway; // 추상 인터페이스에 의존

        // 생성자를 통해 외부에서 구현체를 주입받음
        public OrderService(PaymentGateway paymentGateway) {
            this.paymentGateway = paymentGateway;
        }

        public void processPayment() {
            paymentGateway.pay();
        }
    }
    ```
    *   **장점**: `OrderService`는 `KakaoPay`가 오든 `NaverPay`가 오든 신경 쓰지 않습니다. 코드를 수정할 필요 없이 외부에서 주입해주는 객체만 바꾸면 되므로 매우 유연하고 확장성이 좋습니다.

#### ➕ 의존성 주입 (DI)과 Spring

*   **의존성 주입(Dependency Injection)**은 위와 같이, 필요한 객체(의존성)를 클래스 내부에서 직접 생성하는 것이 아니라, **외부(컨테이너)에서 생성하여 넣어주는(주입하는)** 방식입니다.
*   **Spring의 역할**: Spring의 **DI 컨테이너(IoC 컨테이너)**는 이 "외부"의 역할을 자동으로 수행해줍니다.
    *   개발자는 인터페이스에만 의존하도록 코드를 작성하고, `@Component`, `@Service` 등의 어노테이션을 붙여 Bean으로 등록합니다.
    *   `@Autowired`를 사용하면, Spring 컨테이너가 **자동으로 적절한 구현체(Bean)를 찾아 주입**해줍니다.
    *   즉, Spring은 **DIP 원칙을 매우 쉽고 편리하게 지킬 수 있도록 도와주는 프레임워크**입니다.

---

### 📌 요약

*   **디자인 패턴**은 반복되는 설계 문제에 대한 검증된 해결책으로, **싱글턴, 팩토리, 스트래티지** 패턴 등이 대표적입니다.
*   **SOLID**는 유지보수하기 좋은 객체 지향 설계를 위한 5가지 원칙(**단일 책임, 개방-폐쇄, 리스코프 치환, 인터페이스 분리, 의존관계 역전**)입니다.
*   이 중 **의존관계 역전 원칙(DIP)**은 "구체적인 구현이 아닌 추상적인 인터페이스에 의존하라"는 원칙으로, Spring의 핵심 사상입니다.
*   Spring의 **의존성 주입(DI)**은 이 **DIP 원칙을 자동으로 구현**해주는 매우 강력한 기능으로, 개발자가 유연하고 확장성 있는 코드를 쉽게 작성할 수 있도록 돕습니다.